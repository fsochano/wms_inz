import { ParamExtractorPipe } from './param-extractor.pipe';

describe('ParamExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new ParamExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
